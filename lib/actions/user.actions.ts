"use server";

export const signIn = async () => {
    try{
        // Mutations / Database calls
    }catch(error){
        console.error('Error:', error)
    }
};


export const signUp = async (userData: SignUpParams) => {
    try{
        // Mutations / Database calls
    }catch(error){
        console.error('Error:', error)
    }
}